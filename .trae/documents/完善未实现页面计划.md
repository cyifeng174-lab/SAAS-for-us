# 完善未实现页面计划

## 项目现状分析

### 已实现的页面（11 个）
1. ✅ 登录页面 - `/pages/login/index.vue` - 完整实现
2. ✅ 首页 - `/pages/index/index.vue` - 完整实现
3. ✅ 我的 - `/pages/my/index.vue` - 完整实现
4. ✅ 客户列表 - `/pages/customer/list.vue` - 完整实现
5. ✅ 采购开单 - `/pages/purchase/create.vue` - 完整实现
6. ✅ 加工开单 - `/pages/processing/create.vue` - 完整实现
7. ✅ 销售开单 - `/pages/sales/billing.vue` - 完整实现
8. ✅ 销售历史 - `/pages/sales/history.vue` - 完整实现
9. ✅ 库存详情 - `/pages/inventory/detail.vue` - 完整实现
10. ✅ 库存列表 - `/pages/inventory/list.vue` - 完整实现
11. ✅ 财务收款 - `/pages/finance/receive.vue` - 完整实现

### 缺失的页面（4 个）
1. ❌ 采购列表页面 - `/pages/purchase/list.vue`
2. ❌ 加工列表页面 - `/pages/processing/list.vue`
3. ❌ 财务统计页面 - `/pages/finance/index.vue`
4. ❌ 客户详情/编辑页面 - `/pages/customer/detail.vue`

---

## 实施计划

### 任务 1：创建采购列表页面 `/pages/purchase/list.vue`

**功能需求：**
- 采购订单列表展示（卡片式设计）
- 筛选功能：
  - 日期筛选（今天/昨天/本周/本月/全部）
  - 供应商筛选（半屏弹窗选择）
  - 状态筛选（全部/待入库/已完成/已取消）
- 统计概览：
  - 总采购金额
  - 总入库重量
  - 订单数量
- 订单详情查看（半屏弹窗）
- 支持作废订单
- 支持下拉刷新和上拉加载

**UI 设计要点：**
- 参考销售历史页面设计
- 大按钮、大字体
- 卡片式布局
- 状态标签颜色区分

**后端依赖：**
- 查询采购列表（直接查询 `purchases` 集合）
- 作废采购订单云函数（需创建 `purchase_cancel`）

---

### 任务 2：创建加工列表页面 `/pages/processing/list.vue`

**功能需求：**
- 加工订单列表展示
- 筛选功能：
  - 日期筛选
  - 状态筛选（全部/待入库/已完成/已取消）
- 统计概览：
  - 总加工重量
  - 总出品重量
  - 订单数量
- 订单详情查看（半屏弹窗）
- 显示加工损耗率
- 支持作废订单

**UI 设计要点：**
- 参考采购列表页面设计
- 突出显示损耗率数据
- 批次信息展示

**后端依赖：**
- 查询加工列表（直接查询 `processing_orders` 集合）
- 作废加工订单云函数（需创建 `processing_order_cancel`）

---

### 任务 3：创建财务统计页面 `/pages/finance/index.vue`

**功能需求：**
- 财务流水列表展示
- 筛选功能：
  - 日期筛选
  - 收支类型筛选（全部/收入/支出）
  - 业务类型筛选（采购/销售/加工/收款）
- 统计概览：
  - 总收入
  - 总支出
  - 净利润
- 流水详情查看（半屏弹窗）
- 支持导出功能（预留）

**UI 设计要点：**
- 清晰的收支区分（绿色收入/红色支出）
- 图表展示（预留图表组件位置）
- 时间线样式展示流水

**后端依赖：**
- 查询财务流水列表（查询 `financial_ledgers` 集合）

---

### 任务 4：创建客户详情/编辑页面 `/pages/customer/detail.vue`

**功能需求：**
- 客户基本信息展示
- 客户信息编辑（名称、电话、地址、备注）
- 客户统计信息：
  - 累计采购次数
  - 累计采购金额
  - 当前欠款余额
- 与该客户相关的订单列表（销售历史）
- 删除客户功能（软删除）

**UI 设计要点：**
- 表单编辑模式
- 信息展示清晰
- 操作按钮明显

**后端依赖：**
- 更新客户信息云函数（需创建 `customer_update`）
- 删除客户云函数（需创建 `customer_delete`）

---

## 开发顺序

1. **第一阶段：采购列表页面**（优先级最高）
   - 创建 `/pages/purchase/list.vue`
   - 创建云函数 `purchase_cancel`
   - 更新 `pages.json` 注册页面

2. **第二阶段：加工列表页面**
   - 创建 `/pages/processing/list.vue`
   - 创建云函数 `processing_order_cancel`
   - 更新 `pages.json` 注册页面

3. **第三阶段：财务统计页面**
   - 创建 `/pages/finance/index.vue`
   - 更新底部导航栏添加财务入口
   - 更新 `pages.json` 注册页面

4. **第四阶段：客户详情页面**
   - 创建 `/pages/customer/detail.vue`
   - 创建云函数 `customer_update` 和 `customer_delete`
   - 在客户列表页面添加详情入口

---

## 技术实现要点

### 1. 列表页面通用模式
```javascript
// 分页加载
const currentPage = ref(0)
const pageSize = 20
const hasMore = ref(true)

// 筛选条件
const dateFilterIndex = ref(0)
const selectedSupplierId = ref('')
const filterStatus = ref('')

// 加载数据函数
const loadList = async (isRefresh = false) => {
  // 构建查询条件
  // 分页查询
  // 更新统计数据
}
```

### 2. 半屏弹窗通用模式
```vue
<view class="popup-mask" v-if="showPopup" @click="closePopup"></view>
<view class="popup-container" :class="{ show: showPopup }">
  <view class="popup-header">
    <text class="popup-title">标题</text>
    <text class="popup-close" @click="closePopup">×</text>
  </view>
  <scroll-view class="popup-content" scroll-y>
    <!-- 内容 -->
  </scroll-view>
</view>
```

### 3. 云函数作废订单通用逻辑
```javascript
// 使用事务确保数据一致性
const transaction = await db.startTransaction()
try {
  // 1. 更新订单状态
  await transaction.collection('purchases')
    .doc(orderId)
    .update({ status: 'cancelled', cancel_time: Date.now() })
  
  // 2. 回滚库存（如果有）
  // 3. 提交事务
  await transaction.commit()
  
  return { code: 0, message: 'success' }
} catch (error) {
  await transaction.rollback()
  throw error
}
```

---

## 注意事项

1. **金额精度**：所有金额统一使用"分"为单位存储和计算
2. **事务处理**：涉及库存和财务的操作必须使用数据库事务
3. **数据校验**：前端和后端都需要进行数据校验
4. **错误处理**：完善的错误提示和日志记录
5. **性能优化**：
   - 列表分页加载
   - 搜索防抖处理
   - 图片懒加载（如果有）
6. **用户体验**：
   - 加载状态提示
   - 空状态提示
   - 操作成功/失败反馈
7. **代码规范**：
   - 统一的代码注释风格
   - 遵循项目的代码规范
   - 保持与设计风格一致

---

## 验收标准

- [ ] 所有页面 UI 风格统一，符合"大按钮、大字体、极简操作"原则
- [ ] 所有列表页面支持下拉刷新和上拉加载
- [ ] 所有筛选功能正常工作
- [ ] 所有数据展示准确无误
- [ ] 所有操作有明确的反馈提示
- [ ] 代码包含完整的中文注释
- [ ] 通过真机测试（微信小程序环境）
- [ ] 无控制台错误和警告

---

## 预计工作量

- 采购列表页面：2 小时
- 加工列表页面：2 小时
- 财务统计页面：2.5 小时
- 客户详情页面：2 小时
- 云函数开发：1.5 小时
- 测试和调试：2 小时

**总计：约 12 小时**
